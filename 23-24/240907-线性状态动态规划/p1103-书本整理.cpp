#include <bits/stdc++.h>
using namespace std;
const int N = 1e2 + 10;
int n, k, f[N][N];
int main() {
    cin >> n >> k; k = n - k; // 选 k 本书
    for (int i = 1; i <= n; i++) {
        cin >> x;
    }

    return 0;
}

