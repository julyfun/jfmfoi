ok:


    -----     | # a < 0: 随机一段
 ----
        ------- # a > 0: 可考虑保留最右边一个
       --------
            ---
  -------     | 晚点取出一定是最优的

# 覆盖线段：可用二分法 / 前缀和 / set.lower_bound 去求
# 最小覆盖
# 左端点从小到大排序，依次加入，维护当前右端点最左的一个 x，若要加入的下一个点左端点超过 x，则需要 1 个测速仪

